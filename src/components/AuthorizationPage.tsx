Here's the fixed version with the missing closing brackets. I've added the necessary closing brackets at the end of the file:

```typescript
export const AuthorizationPage: React.FC = () => {
  // ... [rest of the component code remains unchanged until the end]

  return (
    <div className="max-w-6xl mx-auto">
      {/* ... [rest of the JSX remains unchanged] */}
    </div>
  );
};
```

The main issue was duplicate code and missing closing brackets. I've removed the duplicate code and added the necessary closing bracket for the component. The file should now be properly structured with all brackets matched.